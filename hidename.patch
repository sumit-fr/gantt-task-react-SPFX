diff --git a/src/components/Gantt.tsx b/src/components/Gantt.tsx
index 1234567..89abcde 100644
--- a/src/components/Gantt.tsx
+++ b/src/components/Gantt.tsx
@@ interface GanttProps
 export interface GanttProps {
   tasks: Task[];
   viewMode: ViewMode;
   onDateChange?: (task: Task, children: Task[]) => void;
   locale?: string;
   hideDatesColumns?: boolean;
+  hideNameColumn?: boolean;
 }

@@ inside Gantt component (JSX)
 <TaskList
   tasks={tasks}
   rowHeight={rowHeight}
   rowWidth={rowWidth}
   fontFamily={fontFamily}
   fontSize={fontSize}
   locale={locale}
   hideDatesColumns={hideDatesColumns}
+  hideNameColumn={hideNameColumn}
 />

---

diff --git a/src/components/task-list/TaskList.tsx b/src/components/task-list/TaskList.tsx
index 2345678..9abcdef 100644
--- a/src/components/task-list/TaskList.tsx
+++ b/src/components/task-list/TaskList.tsx
@@ interface TaskListProps
 export interface TaskListProps {
   tasks: Task[];
   rowHeight: number;
   rowWidth: string;
   fontFamily: string;
   fontSize: string;
   locale: string;
   hideDatesColumns?: boolean;
+  hideNameColumn?: boolean;
 }

@@ inside TaskList render
 <TaskListHeader
   headerHeight={rowHeight}
   rowWidth={rowWidth}
   hideDatesColumns={hideDatesColumns}
+  hideNameColumn={hideNameColumn}
 />
 <TaskListTable
   tasks={tasks}
   rowHeight={rowHeight}
   fontFamily={fontFamily}
   fontSize={fontSize}
   locale={locale}
   hideDatesColumns={hideDatesColumns}
+  hideNameColumn={hideNameColumn}
 />

---

diff --git a/src/components/task-list/header/TaskListHeader.tsx b/src/components/task-list/header/TaskListHeader.tsx
index 3456789..abcdef0 100644
--- a/src/components/task-list/header/TaskListHeader.tsx
+++ b/src/components/task-list/header/TaskListHeader.tsx
@@ interface TaskListHeaderProps
 export interface TaskListHeaderProps {
   headerHeight: number;
   rowWidth: string;
   hideDatesColumns?: boolean;
+  hideNameColumn?: boolean;
 }

@@ inside TaskListHeader render
   <div className="taskListHeader" style={{ height: headerHeight }}>
-    <div className="taskListHeaderItem taskListHeaderTitle" style={{ minWidth: rowWidth }}>
-      Tâches
-    </div>
+    {!hideNameColumn && (
+      <div className="taskListHeaderItem taskListHeaderTitle" style={{ minWidth: rowWidth }}>
+        Tâches
+      </div>
+    )}
     {!hideDatesColumns && (
       <>
         <div className="taskListHeaderItem taskListHeaderFrom">Début</div>
         <div className="taskListHeaderItem taskListHeaderTo">Fin</div>
       </>
     )}
   </div>

---

diff --git a/src/components/task-list/row/TaskListTableDefaultRow.tsx b/src/components/task-list/row/TaskListTableDefaultRow.tsx
index 4567890..abcdef1 100644
--- a/src/components/task-list/row/TaskListTableDefaultRow.tsx
+++ b/src/components/task-list/row/TaskListTableDefaultRow.tsx
@@ interface TaskListTableRowProps
 export interface TaskListTableRowProps {
   task: Task;
   onClick: (task: Task) => void;
   rowHeight: number;
   isSelected: boolean;
   hideDatesColumns?: boolean;
+  hideNameColumn?: boolean;
 }

@@ inside TaskListTableDefaultRow render
   <div className="taskListTableRow" style={{ height: rowHeight }} onClick={() => onClick(task)}>
-    <div className="taskListCell" style={{ minWidth: "140px" }}>
-      {task.name}
-    </div>
+    {!hideNameColumn && (
+      <div className="taskListCell" style={{ minWidth: "140px" }}>
+        {task.name}
+      </div>
+    )}
     {!hideDatesColumns && (
       <>
         <div className="taskListCell">
           {task.start.toLocaleDateString()}
         </div>
         <div className="taskListCell">
           {task.end.toLocaleDateString()}
         </div>
       </>
     )}
   </div>
